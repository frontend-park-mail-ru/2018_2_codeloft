language: node_js
sudo: false
node_js:
- '9'
addons:
  apt:
    packages:
      - sshpass

script:
  - npm run eslint
after_success:
  - echo "some tests ot commands"
  - chmod ./master_deploy.sh
  - if [ "$TRAVIS_BRANCH" = "master" ]; then ./to_machine_deploy.sh prod; fi
  - if [ "$TRAVIS_BRANCH" = "testing" ]; then ./to_machine_deploy.sh testing; fi 

